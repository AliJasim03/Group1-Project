package com.example.group1bankingapp;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// This is the main class that starts the Spring Boot application.
@SpringBootApplication
public class Group1BankingAppApplication {

	// The main method is the entry point of the application.
	public static void main(String[] args) {
		// This line launches the Spring Boot application.
		SpringApplication.run(Group1BankingAppApplication.class, args);
	}
}
